# Wallpaper directory in $HOME/Wallpapers

env = HYPRCURSOR_THEME,Notwaita-Gray
env = HYPRCURSOR_SIZE,24

execr = hyprpaper
exec-once = ~/.local/share/eww/eww open-many -c /mnt/Programming/Programming/yuck/main_bar profile topbar

monitor = DP-1, 1920x1080@180.00, 0x0, 1
monitor = DVI-D-1, 1366x768, -1366x312, 1

$mainMod = SUPER
$mainExt = SUPER ALT
$mainSt = SUPER SHIFT

$apps = rofi -show drun
$browser = firefox

bind = $mainMod, space, exec, $apps
bind = $mainExt, f, exec, $browser
bind = $mainMod, Q, exec, kitty
bind = $mainMod, C, killactive
bind = $mainMod, F, togglefloating,
bind = $mainSt, F, fullscreen

bind = $mainMod, K, movefocus, d
bind = $mainMod, J, movefocus, u
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Hyprlock
bind = $mainMod CTRL, L, exec, hyprlock

# Screenshot
bind = $mainMod, S, exec, hyprshot -m region -- swayimg
bind = $mainSt, S, exec, hyprshot -m output -m active -- swayimg

bind = $mainMod, Tab, workspace,previous

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod CTRL, 1, workspace, 11
bind = $mainMod CTRL, 2, workspace, 12
bind = $mainMod CTRL, 3, workspace, 13
bind = $mainMod CTRL, 4, workspace, 14
bind = $mainMod CTRL, 5, workspace, 15
bind = $mainMod CTRL, 6, workspace, 16
bind = $mainMod CTRL, 7, workspace, 17
bind = $mainMod CTRL, 8, workspace, 18
bind = $mainMod CTRL, 9, workspace, 19
bind = $mainMod CTRL, 0, workspace, 20


# move window
bind = $mainSt, H, movewindow, l
bind = $mainSt, L, movewindow, r
bind = $mainSt, J, movewindow, u
bind = $mainSt, K, movewindow, d

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainSt, 1, movetoworkspace, 1
bind = $mainSt, 2, movetoworkspace, 2
bind = $mainSt, 3, movetoworkspace, 3
bind = $mainSt, 4, movetoworkspace, 4
bind = $mainSt, 5, movetoworkspace, 5
bind = $mainSt, 6, movetoworkspace, 6
bind = $mainSt, 7, movetoworkspace, 7
bind = $mainSt, 8, movetoworkspace, 8
bind = $mainSt, 9, movetoworkspace, 9
bind = $mainSt, 0, movetoworkspace, 10


bind = $mainMod CTRL SHIFT, 1, movetoworkspace, 11
bind = $mainMod CTRL SHIFT, 2, movetoworkspace, 12
bind = $mainMod CTRL SHIFT, 3, movetoworkspace, 13
bind = $mainMod CTRL SHIFT, 4, movetoworkspace, 14
bind = $mainMod CTRL SHIFT, 5, movetoworkspace, 15
bind = $mainMod CTRL SHIFT, 6, movetoworkspace, 16
bind = $mainMod CTRL SHIFT, 7, movetoworkspace, 17
bind = $mainMod CTRL SHIFT, 8, movetoworkspace, 18
bind = $mainMod CTRL SHIFT, 9, movetoworkspace, 19
bind = $mainMod CTRL SHIFT, 0, movetoworkspace, 20

bind = $mainMod CTRL, P, exec, hyprpicker | wl-copy

# Disable second monitor
bind = $mainSt CTRL, D, exec, hyprctl keyword monitor "DVI-D-1, disabled"

bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-

# Plugin bindables
bind = $mainMod, a, hyprexpo:expo, toggle


# workspaces
workspace=1,monitor:DP-1
workspace=2,monitor:DP-1
workspace=3,monitor:DP-1
workspace=4,monitor:DP-1
workspace=5,monitor:DP-1
workspace=6,monitor:DP-1
workspace=7,monitor:DP-1
workspace=8,monitor:DP-1
workspace=9,monitor:DP-1
workspace=10,monitor:DP-1
workspace=11,monitor:DVI-D-1
workspace=12,monitor:DVI-D-1
workspace=13,monitor:DVI-D-1
workspace=14,monitor:DVI-D-1
workspace=15,monitor:DVI-D-1
workspace=16,monitor:DVI-D-1
workspace=17,monitor:DVI-D-1
workspace=18,monitor:DVI-D-1
workspace=19,monitor:DVI-D-1

# -------------------------------------- DESIGN -------------------------------------- 

source = colors.conf

general {
  #col.active_border = rgb($primary)
  #col.inactive_border = rgb($surface)
  gaps_in = 2
  gaps_out = 2
  border_size = 0
  layout = dwindle
}

cursor {
  no_warps=false
}

misc {
  disable_hyprland_logo=true
}

decoration {
  blur {
      enabled = yes
      ignore_opacity = true
      size = 4
      passes = 2
      new_optimizations = yes
      contrast = 0.7
      brightness = 0.7
      popups = true
      input_methods = true
  }
  rounding = 5
  shadow {
    enabled = false
    range = 10
    render_power = 2
    color = rgba(0, 0, 0, 0.25)
  }
}

animations {
  enabled = yes
  bezier = myBezier, 0.4, 0.0, 0.2, 1.0
  bezier = easeOutCubic, 0.215, 0.61, 0.355, 1
  animation = windowsIn, 1, 2, myBezier, slide bottom 50%
  animation = windowsOut, 1, 2, myBezier, slide top 
  animation = windowsMove, 1, 1.2, easeOutCubic
  animation = border, 1, 2, myBezier
  animation = fade, 1, 2, myBezier
  animation = workspaces, 1, 2, myBezier, slidefadevert 60%
}

input{
  kb_layout = us,ru
  kb_options = grp:alt_shift_toggle
  accel_profile = flat
  sensitivity = 0
}

plugin {
  hyprexpo {
    enabled = true
    columns = 3
    gap_size = 5
    bg_col = rgb(111111)
    workspace_method = center current
  }

  dynamic-cursors {
    enabled = true
    mode = stretch
    threshold = 1

    # for mode = rotate
    rotate {

        # length in px of the simulated stick used to rotate the cursor
        # most realistic if this is your actual cursor size
        length = 20

        # clockwise offset applied to the angle in degrees
        # this will apply to ALL shapes
        offset = 0.0
    }

    # for mode = tilt
    tilt {

        # controls how powerful the tilt is, the lower, the more power
        # this value controls at which speed (px/s) the full tilt is reached
        # the full tilt being 60° in both directions
        limit = 7500

        # relationship between speed and tilt, supports these values:
        # linear             - a linear function is used
        # quadratic          - a quadratic function is used (most realistic to actual air drag)
        # negative_quadratic - negative version of the quadratic one, feels more aggressive
        # see `activation` in `src/mode/utils.cpp` for how exactly the calculation is done
        function = negative_quadratic

        # time window (ms) over which the speed is calculated
        # higher values will make slow motions smoother but more delayed
        window = 300
    }

    # for mode = stretch
    stretch {

        # controls how much the cursor is stretched
        # this value controls at which speed (px/s) the full stretch is reached
        # the full stretch being twice the original length
        limit = 3000

        # relationship between speed and stretch amount, supports these values:
        # linear             - a linear function is used
        # quadratic          - a quadratic function is used
        # negative_quadratic - negative version of the quadratic one, feels more aggressive
        # see `activation` in `src/mode/utils.cpp` for how exactly the calculation is done
        function = quadratic

        # time window (ms) over which the speed is calculated
        # higher values will make slow motions smoother but more delayed
        window = 100
    }

    # configure shake to find
    # magnifies the cursor if its is being shaken
    shake {

        # enables shake to find
        enabled = false

        # use nearest-neighbour (pixelated) scaling when shaking
        # may look weird when effects are enabled
        nearest = true

        # controls how soon a shake is detected
        # lower values mean sooner
        threshold = 6.0

        # magnification level immediately after shake start
        base = 4.0
        # magnification increase per second when continuing to shake
        speed = 4.0
        # how much the speed is influenced by the current shake intensitiy
        influence = 0.0

        # maximal magnification the cursor can reach
        # values below 1 disable the limit (e.g. 0)
        limit = 0.0

        # time in millseconds the cursor will stay magnified after a shake has ended
        timeout = 2000

        # show cursor behaviour `tilt`, `rotate`, etc. while shaking
        effects = false

        # enable ipc events for shake
        # see the `ipc` section below
        ipc = false
    }

    # use hyprcursor to get a higher resolution texture when the cursor is magnified
    # see the `hyprcursor` section below
    hyprcursor {

        # use nearest-neighbour (pixelated) scaling when magnifing beyond texture size
        # this will also have effect without hyprcursor support being enabled
        # 0 / false - never use pixelated scaling
        # 1 / true  - use pixelated when no highres image
        # 2         - always use pixleated scaling
        nearest = true

        # enable dedicated hyprcursor support
        enabled = true

        # resolution in pixels to load the magnified shapes at
        # be warned that loading a very high-resolution image will take a long time and might impact memory consumption
        # -1 means we use [normal cursor size] * [shake:base option]
        resolution = -1

        # shape to use when clientside cursors are being magnified
        # see the shape-name property of shape rules for possible names
        # specifying clientside will use the actual shape, but will be pixelated
        fallback = clientside
    }
  }
}


windowrule=workspace 5, class: org.telegram.desktop
windowrule=workspace 2, class: firefox
windowrule=workspace 10, class: discord
windowrule=workspace 6, class: jetbrains-rider
windowrule=workspace 9, class: obsidian

layerrule = blur, eww
